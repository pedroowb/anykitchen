<div class="container min-vh-100 d-flex flex-column justify-content-center align-items-center">
  <div class="card p-5 shadow rounded-4 border-0" style="max-width: 400px; background-color: #e9f8ec;">
    <h2 class="mb-4 fw-bold text-success text-center">Login</h2>

    <%= form_with url: login_path, method: :post, local: true do |form| %>
      <div class="mb-3">
        <%= form.label :email, "Usuário", class: "form-label" %>
        <%= form.text_field :email, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :password, "Senha", class: "form-label" %>
        <%= form.password_field :password, class: "form-control" %>
      </div>

      <%= form.submit "Entrar", class: "btn btn-success btn-lg w-100 rounded-pill" %>
    <% end %>
  </div>
</div>
